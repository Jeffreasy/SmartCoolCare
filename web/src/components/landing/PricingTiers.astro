---
import Tilt3D from "../ui/Tilt3D.astro";
import { Check } from "lucide-react";
const plans = [
    {
        name: "Basis",
        price: "€149",
        description: "Essential compliance & automation for kitchens.",
        color: "text-status-success",
        borderColor: "border-status-success/20",
        btnClass:
            "bg-status-success/10 hover:bg-status-success/20 text-status-success",
        features: [
            "24/7 Temperature Logging",
            "15-min Interval",
            "Cloud Dashboard",
            "HACCP Compliant Reports",
            "WiFi Connectivity",
        ],
    },
    {
        name: "Pro",
        price: "€199",
        popular: true,
        description: "Advanced prevention & alerts for medication storage.",
        color: "text-status-warning",
        borderColor: "border-status-warning/30",
        btnClass:
            "btn-primary bg-status-warning hover:bg-status-warning/90 border-none text-slate-900 shadow-status-warning/20",
        features: [
            "All Basis Features",
            "Door Open Detection (>60s)",
            "Local Audio Alarm (Buzzer)",
            "Email/Push Alerts",
            "Actionable Insights",
        ],
    },
    {
        name: "Elite",
        price: "€249",
        description: "Fail-safe protection for high-value assets (Vaccines).",
        color: "text-status-error",
        borderColor: "border-status-error/20",
        btnClass:
            "bg-status-error/10 hover:bg-status-error/20 text-status-error",
        features: [
            "All Pro Features",
            "Power-loss Detection",
            "Battery Backup (Black Box)",
            "Humidity & Pressure Monitoring",
            "Critical Asset Protection protection",
        ],
    },
];
---

<section id="pricing" class="py-24 relative z-10">
    <div class="max-w-7xl mx-auto px-4">
        <div class="text-center mb-16">
            <h2
                class="text-3xl md:text-5xl font-bold bg-gradient-to-r from-white to-slate-400 bg-clip-text text-transparent mb-4"
            >
                Simple, Transparent Pricing
            </h2>
            <p class="text-slate-400 text-lg">
                One-time hardware purchase. No mandatory monthly fees.
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-start">
            {
                plans.map((plan) => (
                    <Tilt3D>
                        <div
                            class={`glass-panel p-6 md:p-8 relative transition-all duration-300 h-full flex flex-col ${plan.popular ? "border-status-warning/30 bg-status-warning/5 shadow-lg shadow-status-warning/10" : "border-white/10"}`}
                        >
                            {plan.popular && (
                                <div class="absolute -top-4 left-1/2 -translate-x-1/2 bg-status-warning text-black font-bold text-xs px-3 py-1 rounded-full uppercase tracking-wider shadow-lg transform transition-transform duration-500 group-hover:translate-z-20">
                                    Recommended
                                </div>
                            )}

                            <div class="text-center mb-8 transform transition-transform duration-500 group-hover:translate-z-10">
                                <h3
                                    class={`text-xl font-bold mb-2 ${plan.color}`}
                                >
                                    {plan.name}
                                </h3>
                                <div class="text-4xl font-extrabold text-white mb-2">
                                    {plan.price}
                                </div>
                                <p class="text-slate-500 text-sm h-10">
                                    {plan.description}
                                </p>
                            </div>

                            <ul class="space-y-4 mb-8 flex-1 transform transition-transform duration-500 group-hover:translate-z-10">
                                {plan.features.map((feature) => (
                                    <li class="flex items-center gap-3 text-slate-300 text-sm">
                                        <Check
                                            className={`w-5 h-5 flex-shrink-0 ${plan.color}`}
                                        />
                                        {feature}
                                    </li>
                                ))}
                            </ul>

                            <a
                                href="/signup"
                                class={`block w-full text-center py-3 rounded-xl font-bold transition-all transform hover:scale-105 active:scale-95 duration-200 ${plan.btnClass}`}
                            >
                                Order Now
                            </a>
                        </div>
                    </Tilt3D>
                ))
            }
        </div>

        <div class="text-center mt-12 text-slate-500 text-sm">
            Need a custom enterprise setup? <a
                href="#"
                class="text-brand-primary underline decoration-brand-primary/30 underline-offset-4"
                >Contact Sales</a
            >
        </div>
    </div>
</section>
